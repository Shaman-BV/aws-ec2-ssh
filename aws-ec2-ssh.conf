#edit group for ssh access
IAM_AUTHORIZED_GROUPS="shaman-group-dev-ssh"
LOCAL_MARKER_GROUP="iam-synced-users"
LOCAL_GROUPS=""
#edit group for sudoers
SUDOERS_GROUPS="shaman-group-dev-ssh"
ASSUMEROLE=""

# Remove or set to 0 if you are done with configuration
# To change the interval of the sync change the file
# /etc/cron.d/import_users
DONOTSYNC=0
